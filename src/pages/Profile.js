import React from "react";
import ProfileContent from "../components/ProfileContent";

const Profile = (props) => {
  return (
    <div>
      <ProfileContent />
    </div>
  );
};

export default Profile;
